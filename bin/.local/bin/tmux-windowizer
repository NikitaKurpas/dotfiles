#!/usr/bin/env bash

tmux neww
session_name=$(tmux list-windows -a -F "#S:#W" | fzf -q "$1")

if [ -z $session_name ]; then
    exit 0
fi

if [ -z $TMUX ]; then 
   tmux attach-session -t "=$session_name"
else
   tmux switch-client -t "=$session_name"
fi
